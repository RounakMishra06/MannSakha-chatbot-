{
  "name": "mannsakha-chatbot",
  "version": "1.0.0",
  "description": "AI-powered mental health support chatbot with multi-API backend",
  "main": "backend/index.js",
  "type": "module",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "cd backend && npm install && cd ../frontend && npm install",
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "test": "cd backend && npm test"
  },
  "keywords": [
    "chatbot",
    "ai",
    "mental-health",
    "nodejs",
    "express",
    "gemini",
    "openai"
  ],
  "author": "Rounak Mishra",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.15.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}